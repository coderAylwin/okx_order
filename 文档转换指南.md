# 文档转换指南

## 将 Markdown 转换为其他格式

### 方法1：使用 Pandoc（推荐，功能最全）

#### 安装 Pandoc
```bash
# macOS
brew install pandoc

# 或使用 pip
pip install pypandoc
```

#### 转换为 PDF
```bash
# 需要先安装 LaTeX（用于PDF生成）
# macOS
brew install --cask basictex

# 转换命令
pandoc 加密货币多因子数据分析报告.md -o 加密货币多因子数据分析报告.pdf \
  --pdf-engine=xelatex \
  -V mainfont="PingFang SC" \
  -V geometry:margin=2cm \
  --toc
```

#### 转换为 Word (DOCX)
```bash
pandoc 加密货币多因子数据分析报告.md -o 加密货币多因子数据分析报告.docx \
  --toc \
  --reference-doc=template.docx  # 可选：使用自定义模板
```

#### 转换为 HTML
```bash
pandoc 加密货币多因子数据分析报告.md -o 加密货币多因子数据分析报告.html \
  --standalone \
  --css=style.css  # 可选：自定义样式
```

#### 转换为 PPT (需要先转换为 HTML，再导入)
```bash
# 先转换为HTML
pandoc 加密货币多因子数据分析报告.md -o 加密货币多因子数据分析报告.html --standalone

# 然后在PowerPoint中：文件 -> 打开 -> 选择HTML文件
```

---

### 方法2：使用 Python 脚本转换

#### 安装依赖
```bash
pip install markdown pypandoc python-docx reportlab
```

#### Python 转换脚本
```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Markdown文档转换工具
支持转换为：PDF、Word、HTML
"""

import os
import sys

def convert_to_pdf(md_file, output_file=None):
    """转换为PDF"""
    try:
        import pypandoc
        if output_file is None:
            output_file = md_file.replace('.md', '.pdf')
        
        pypandoc.convert_file(
            md_file,
            'pdf',
            outputfile=output_file,
            extra_args=['--pdf-engine=xelatex', '-V', 'mainfont=PingFang SC']
        )
        print(f"✅ PDF已生成: {output_file}")
        return True
    except Exception as e:
        print(f"❌ PDF转换失败: {e}")
        return False

def convert_to_docx(md_file, output_file=None):
    """转换为Word"""
    try:
        import pypandoc
        if output_file is None:
            output_file = md_file.replace('.md', '.docx')
        
        pypandoc.convert_file(md_file, 'docx', outputfile=output_file)
        print(f"✅ Word已生成: {output_file}")
        return True
    except Exception as e:
        print(f"❌ Word转换失败: {e}")
        return False

def convert_to_html(md_file, output_file=None):
    """转换为HTML"""
    try:
        import markdown
        if output_file is None:
            output_file = md_file.replace('.md', '.html')
        
        with open(md_file, 'r', encoding='utf-8') as f:
            md_content = f.read()
        
        html = markdown.markdown(md_content, extensions=['toc', 'tables', 'fenced_code'])
        
        html_template = f"""<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>加密货币多因子数据分析报告</title>
    <style>
        body {{ font-family: "PingFang SC", "Microsoft YaHei", sans-serif; max-width: 1200px; margin: 0 auto; padding: 20px; }}
        h1 {{ color: #2c3e50; border-bottom: 3px solid #3498db; padding-bottom: 10px; }}
        h2 {{ color: #34495e; margin-top: 30px; }}
        table {{ border-collapse: collapse; width: 100%; margin: 20px 0; }}
        th, td {{ border: 1px solid #ddd; padding: 12px; text-align: left; }}
        th {{ background-color: #3498db; color: white; }}
        code {{ background-color: #f4f4f4; padding: 2px 6px; border-radius: 3px; }}
        pre {{ background-color: #f4f4f4; padding: 15px; border-radius: 5px; overflow-x: auto; }}
    </style>
</head>
<body>
{html}
</body>
</html>"""
        
        with open(output_file, 'w', encoding='utf-8') as f:
            f.write(html_template)
        
        print(f"✅ HTML已生成: {output_file}")
        return True
    except Exception as e:
        print(f"❌ HTML转换失败: {e}")
        return False

if __name__ == '__main__':
    md_file = '加密货币多因子数据分析报告.md'
    
    if not os.path.exists(md_file):
        print(f"❌ 文件不存在: {md_file}")
        sys.exit(1)
    
    print("选择转换格式:")
    print("1. PDF")
    print("2. Word (DOCX)")
    print("3. HTML")
    print("4. 全部")
    
    choice = input("请输入选项 (1-4): ").strip()
    
    if choice == '1':
        convert_to_pdf(md_file)
    elif choice == '2':
        convert_to_docx(md_file)
    elif choice == '3':
        convert_to_html(md_file)
    elif choice == '4':
        convert_to_pdf(md_file)
        convert_to_docx(md_file)
        convert_to_html(md_file)
    else:
        print("❌ 无效选项")
```

---

### 方法3：在线转换工具（最简单）

#### 推荐网站
1. **Dillinger** - https://dillinger.io/
   - 在线Markdown编辑器
   - 支持导出为HTML、PDF、Markdown

2. **Markdown to PDF** - https://www.markdowntopdf.com/
   - 直接上传MD文件，转换为PDF

3. **CloudConvert** - https://cloudconvert.com/md-to-pdf
   - 支持多种格式转换

#### 使用步骤
1. 打开在线工具
2. 上传或粘贴Markdown内容
3. 选择输出格式（PDF/Word/HTML）
4. 下载转换后的文件

---

### 方法4：VS Code 插件（适合开发者）

#### 安装插件
1. **Markdown PDF** - 直接导出PDF
2. **Markdown Preview Enhanced** - 预览和导出多种格式

#### 使用步骤
1. 在VS Code中打开Markdown文件
2. 右键点击文件
3. 选择"Markdown PDF: Export (pdf)" 或 "Markdown PDF: Export (html)"

---

### 方法5：用于PPT分享（推荐方法）

#### 方法A：直接复制到PPT
1. 打开Markdown文件
2. 使用支持Markdown的编辑器（如Typora、Mark Text）
3. 复制内容到PPT（保持格式）

#### 方法B：转换为HTML后导入PPT
```bash
# 转换为HTML
pandoc 加密货币多因子数据分析报告.md -o report.html --standalone

# 在PowerPoint中：
# 文件 -> 打开 -> 选择HTML文件
# 或：插入 -> 对象 -> 文本文件 -> 选择HTML
```

#### 方法C：分段提取内容
创建一个Python脚本，将Markdown按章节提取，便于制作PPT：

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
提取Markdown内容用于PPT制作
"""

import re

def extract_sections(md_file):
    """提取各个章节内容"""
    with open(md_file, 'r', encoding='utf-8') as f:
        content = f.read()
    
    # 按 ## 分割章节
    sections = re.split(r'\n## ', content)
    
    for i, section in enumerate(sections[1:], 1):  # 跳过第一个（标题）
        # 提取章节标题
        title_match = re.match(r'^(.+?)\n', section)
        if title_match:
            title = title_match.group(1)
            print(f"\n{'='*60}")
            print(f"章节 {i}: {title}")
            print(f"{'='*60}")
            print(section[:500] + "..." if len(section) > 500 else section)
            print()

if __name__ == '__main__':
    extract_sections('加密货币多因子数据分析报告.md')
```

---

### 方法6：使用 Typora（最推荐，所见即所得）

#### 安装 Typora
- 官网：https://typora.io/
- 支持直接导出为PDF、Word、HTML、图片等

#### 使用步骤
1. 用Typora打开Markdown文件
2. 文件 -> 导出 -> 选择格式（PDF/Word/HTML）
3. 设置样式和选项
4. 导出

---

## 快速转换命令（一键执行）

### 创建转换脚本
```bash
# 创建 convert.sh
cat > convert.sh << 'EOF'
#!/bin/bash

MD_FILE="加密货币多因子数据分析报告.md"

echo "开始转换..."

# 转换为PDF（需要pandoc和xelatex）
if command -v pandoc &> /dev/null; then
    pandoc "$MD_FILE" -o "${MD_FILE%.md}.pdf" \
        --pdf-engine=xelatex \
        -V mainfont="PingFang SC" \
        -V geometry:margin=2cm \
        --toc && echo "✅ PDF已生成"
fi

# 转换为Word
if command -v pandoc &> /dev/null; then
    pandoc "$MD_FILE" -o "${MD_FILE%.md}.docx" --toc && echo "✅ Word已生成"
fi

# 转换为HTML
if command -v pandoc &> /dev/null; then
    pandoc "$MD_FILE" -o "${MD_FILE%.md}.html" \
        --standalone \
        --css=https://cdn.jsdelivr.net/npm/github-markdown-css@5/github-markdown.min.css \
        && echo "✅ HTML已生成"
fi

echo "转换完成！"
EOF

chmod +x convert.sh
./convert.sh
```

---

## 推荐方案

### 对于技术分享PPT
1. **最简单**：使用Typora打开，直接导出为PDF，然后导入PPT
2. **最专业**：使用Pandoc转换为HTML，然后在PPT中插入
3. **最灵活**：分段提取内容，手动制作PPT（可以更好地控制布局）

### 对于文档分享
1. **PDF**：使用Pandoc或Typora导出
2. **Word**：使用Pandoc转换
3. **在线分享**：转换为HTML，部署到网站

---

## 注意事项

1. **中文字体**：转换PDF时确保系统有中文字体（如PingFang SC、Microsoft YaHei）
2. **表格格式**：Markdown表格在转换时可能需要调整
3. **代码块**：确保代码块语法正确
4. **图片**：如果文档中有图片，确保图片路径正确

---

需要我帮您创建一个一键转换脚本吗？

